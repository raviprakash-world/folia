# Used by docker-compose.yml for variable substitution.
# Copy to .env and fill in real values before running `docker compose up`.

POSTGRES_PASSWORD=folia_dev_password
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
CORS_ORIGINS=http://localhost:5173
