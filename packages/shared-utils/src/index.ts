// @folia/shared-utils
//
// Pure utility functions genuinely shared between apps/web and apps/api.
// Empty at Phase 0 — apps/web and apps/api currently have no real
// overlapping logic to factor out; forcing a shared abstraction before
// one exists would be premature. Populate this only when a specific,
// concrete duplication is found between the two apps.

export {};
