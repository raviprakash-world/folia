// @folia/api-client
//
// A typed client wrapping calls to the real API, meant to eventually
// replace apps/web's direct axios usage (apps/web/src/services/*) one
// service at a time — see the root README's "Frontend integration
// strategy" for the planned migration order. Empty at Phase 0: there are
// no real endpoints to wrap yet (Phase 1 adds the first ones).

export {};
