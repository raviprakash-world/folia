// @folia/shared-types
//
// Types shared between apps/web and apps/api — the contract both sides
// agree on. Deliberately empty at Phase 0: the first real shared types
// (User, AuthTokens, and the DTOs for each endpoint) belong to Phase 1
// (Authentication & Users), per this project's own phase breakdown.
// Adding them here now would mean starting Phase 1 before Phase 0 has
// been reviewed.
//
// Usage once populated (from either app):
//   import type { User } from '@folia/shared-types';

export {};
